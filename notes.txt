Tâches à faire :
    Rendre le chatbox responsives quand la taille est réduite
    Ajouter des chat room (salons) 
    Faire fonctionner le bouton du header quand la taille de la page est réduite
    
    Voir les users connectés ( les ajouter dans une liste puis les enlever quand ils se déconnectent)
    Afficher l'historique que pour les nouveaux users ( pas les users deja connectés)

Problèmes rencontrés :
    L'historique qui s'affiche dans le chat de tout le monde quand un nouveaux utilsateur se connecte ( duplication)
    
Audio :
    Avec MediaThemeTailwindAudio, pour ajouter une audio depuis notre librairie, 
    il faut mettre le fichier audio dans le dossier "public"

Pour Transporter une variable d'une page à une autre (voir la page chat) :
    on utilise searchParams.get pour récupérer la valuer importer.
    Puis, on ajoute la valuer importer dans une string dans ${} ==> '<p>${valeurImporte}</p>'. 
    Avec innerHTML, en ajoute la string dans une div 


Grid tailwind css: 
    Quand on veut appliquer une position sur touts les grids items ==> place-items-...
    Quand on veut appliquer une position sur un grid item précis ==> place-self-...
    Quand on veut appliquer une position sur un grid item précis avec des positions différentes sur les deux axes ==> justify-self-... pour l'axe horizontal && self-... pour l'axe vertical


Fonctions que je veux ajouter : 
    Icons et photos de profil
    pouvoir changer l'arrière plan du chatbox    


Lien pour le tuto Socket.io :
https://blog.logrocket.com/implementing-websocket-communication-next-js/

Quand on veut avoir la valeur d'une variable dont la valeur contient .getElementById("") :
    On utilise HTMLInputElement ==> (document.getElementById(elementId) as HTMLInputElement).value